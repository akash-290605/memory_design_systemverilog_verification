
### `test_scenarios/invalid_test.md`

```markdown
# INVALID_TEST

## Purpose

The `INVALID_TEST` verifies the behavior of the memory DUT when an invalid transaction is presented.

## Stimulus

The test generates transactions with:

```text
valid = 0