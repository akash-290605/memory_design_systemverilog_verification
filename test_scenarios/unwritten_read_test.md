
### `test_scenarios/unwritten_read_test.md`

```markdown
# UNWRITTEN_READ_TEST

## Purpose

The `UNWRITTEN_READ_TEST` verifies the behavior of reading from a memory address that has not previously been written.

## Stimulus

A read operation is performed on an unwritten address.

Example:

```text
Read Address = 150